#ifndef TREE_OF_POSSIBILITIES_HPP
#define TREE_OF_POSSIBILITIES_HPP
#include <vector>
#include <string>

int claculatePosibilities (const std::vector<int> &message,const int &index = 0);
int isValid (const int &num_1, const int &num_2);

#endif
